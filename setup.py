#if you want to install the local package in your system the you need to write this
from setuptools import setup,find_packages

setup(
    name="us_visa",
    version="0.0.0",
    author="subha",
    author_email="ssubhalaxmi28j@gmail.com",
    packages=find_packages()
)